# 饥荒 Mod 研发与执行流水日志 (LOGS)

## 2026-08-16
- **15:42**: 修复 `AddPlayerPostInit` 生命周期中首帧 `TheWorld == nil` 导致的开局发书静默失效问题，将判断逻辑收敛进 `inst:DoTaskInTime(0.5, ...)` 内部。
- **15:45**: 彻底消除 `modmain.lua` 顶部的 `local TheWorld = GLOBAL.TheWorld` 静态空指针缓存，改用动态运行时访问与 `TheNet:GetIsServer()` 安全判定。
- **15:48**: 修复 `atlas_todolist.lua` 中在非 modmain 组件环境下错误调用 `GLOBAL.json` 触发严格模式未声明变量崩溃的问题，彻底理清 `_G` 与 `GLOBAL` 的作用域边界。
- **15:51**: 攻坚无头离线测试链路，通过配置 `-offline` 成功绕过 Steam 令牌限制，在后台自动化完成“世界生成 ➔ 实体注入 ➔ 后端运行 (`LOAD BE`)”的全量仿真闭环。
- **15:53**: 从创意工坊成熟蓝本找回并缝合官方标准的 `COMPONENT_ACTIONS.INVENTORY.book` 与 `ACTIONS.READ.fn` 钩子，打通全角色右键动作收集。
- **15:55**: 修复 `atlasbook_ui.lua` 中 `require("strings")` 撞名官方根目录文件返回布尔值导致的类型索引报错，改用安全受控的字典读取。
- **16:07**: 确立并持久化沉淀《饥荒 Mod 开发与 UI 测试方法论》至 `.agent/5. MEMORY.md`，明确“无头后台后端防御 + 游戏内轻量探针 + F5 秒级热重载”的无痛开发闭环。
- **16:24**: 攻克官方 `WriteableWidget` 原生木牌弹窗的退场时序与关闭死锁问题，改用 `ThePlayer.HUD:OpenWriteableWidget` 接入官方登记体系，实现“确定/取消/点击外部背景”丝滑关闭。
- **16:40**: 完成多玩家客机 `player.userid` 私有化账本隔离改造与统一化 ModRPC 通信回路。
- **16:45 (终极里程碑：双列 Todo 升级完成)**:
  1. 彻底剔除 750 行静态攻略旧代码（`scripts/strings.lua` 与 `data/guides`）。
  2. 实现服务端 `personal_tasks[userid]` 与 `team_tasks` 物理分池与单播/广播双通道分发。
  3. 构建纯净双列任务看板 UI（左列：个人私密计划，右列：团队协同目标）。
  4. 通过离线无头全真服务器仿真校验与 Git 封板提交（Commit `8731db3`）。
- **17:05 (多模态混合视图架构升级)**:
  1. 支持【双列全景 ⇄ 单列超宽聚焦】右上角单键无缝切换。
  2. 单列模式支持顶部并排子 Tab 切换（个人计划 ⇄ 团队目标），提供超大文字呼吸感。
- **17:20 (视觉与生命周期终局打磨)**:
  1. 根治世界存档持久化生命周期，将组件挂载收敛至 `AddPrefabPostInit("world", ...)`。
  2. 修复字模未命中问号乱码，全面使用标准根号 `√` 与大写 `X`。
  3. 将“看 板”主标题精准内嵌至顶部金色木质牌匾中，实现官方 DLC 级质感。
  4. 构筑并嵌入 8 大项多玩家联机端到端全真自动化压测探针。
- **17:55 (质量更新第一阶段：官方 ScrollableList 滚轮长列表接入)**:
  1. 深入调研官方 `scripts/widgets/scrollablelist.lua` 底层源码与实装案例。
  2. 彻底重构双列与单列任务列表为原生 `ScrollableList`，完美解决 20+ 条任务屏幕溢出痛点。
  3. 支持鼠标滚轮丝滑滚动、视口自动遮罩、自适应滚动条收放，通过无头仿真测试并提交 Git（Commit `957adb6`）。
- **18:08 (质量更新第二阶段：UI 动态极速秒开模式与双端 StateGraph 拦截)**:
  1. 消除原版 1.5 秒举书施法动作硬直，StateGraph 动态切换为 0.1s 轻微动作瞬间弹窗。
  2. 看板 UI 左上角新增 `[ ⚡秒开:开 ]` / `[ 📖施法:开 ]` 实时动态点选开关。
  3. `modinfo.lua` 新增 `reading_anim` 配置项，默认设为极速秒开（版本升级至 5.2，Commit `bfc6fdc`）。
